           /* what */
import {* as whatever} from 'whatever';

/**/

 /**/



class Test                     extends Base {
    foo       ;
    bar        = 123;
             effectivelyConst = 'I will not change';
}

const test = void 0;



function foo   (a       ,b        )           {
    const c = (window        ) + a;
    const c =        window + a;

    let foo = 1 > 0 ? 'yes!' : 'no';
    return `${ b }${ c }`;
}